# Mustry Academy — Lab 06 (secrets, migrations, modules & JARs)
# Three Ignition 8.3 gateways + one TimescaleDB with three logical databases:
#
#   gateway-local-development    http://localhost:8088   bind-mounts ./projects + ./services/config
#                                          (working gateway — edits show
#                                           up immediately on disk)
#   gateway-test    http://localhost:8089   projects/ + config/ bind-mounted from
#                                          ./gateways/test, populated by deploy.yml
#   gateway-production   http://localhost:8090   projects/ + config/ bind-mounted from
#                                          ./gateways/production, populated by deploy.yml
#                                          (workflow_dispatch with target=production)
#
#   timescaledb    localhost:5432          databases: ignition_local_development, ignition_test,
#                                          ignition_production (one per env), plus the
#                                          read-only `reporting` login (db-init/)
#
# Run it:
#   cp .env.example .env
#   scripts/setup.sh           # or:  docker compose up -d
# Then:
#   open http://localhost:8088   (local)
#   open http://localhost:8089   (test — empty until the first deploy run)
#   open http://localhost:8090   (production — empty until the first deploy run)
#
# NOTE (Part 1A of the lab): the database passwords below enter the containers
# as ENVIRONMENT VARIABLES — anyone who can `docker inspect` reads them in
# plaintext. Part 1 of the lab moves them to file-based secrets: a top-level
# `secrets:` block backed by ./secrets/*.txt, POSTGRES_PASSWORD_FILE +
# REPORTING_PASSWORD_FILE on the DB service, and BOTH secrets attached to BOTH
# services (the DB service also because db-init/ reads the reporting file on a
# fresh volume; the gateway because its secret provider resolves them). That
# edit is the exercise, so this file ships WITHOUT it.

---
# `name:` pins the Compose project so this stack always resolves to THESE
# containers and volumes, whichever directory you run compose from. Without it
# the project name comes from the folder name, so a second clone of this lab
# (a fork next to the course repo, a scratch checkout) silently adopts the other
# one's containers and its timescaledb volume — and db-init/, which only runs on
# a first volume init, never runs for the new clone.
name: cicd-lab06

# YAML anchors for the bits that don't differ across the three gateways. Each
# service merges these in and overrides the parts that do (ports, admin creds,
# the gateway name passed to the wrapper, volumes).
x-ignition-env: &ignition-env
  ACCEPT_IGNITION_EULA: "Y"
  IGNITION_EDITION: standard
  # Skip the Quick Start prompt on first login — the lab ships its own projects.
  DISABLE_QUICKSTART: "true"
  GATEWAY_MODULES_ENABLED: >-
    com.inductiveautomation.alarm-notification,
    com.inductiveautomation.eventstream,
    com.inductiveautomation.historian,
    com.inductiveautomation.connectors.kafka,
    com.inductiveautomation.opcua.drivers.modbus,
    com.inductiveautomation.opcua,
    com.inductiveautomation.perspective,
    com.inductiveautomation.reporting,
    com.inductiveautomation.historian.sql,
    com.inductiveautomation.symbol-factory,
    com.inductiveautomation.opcua.drivers.tcpudp,
    com.inductiveautomation.webdev,
    com.inductiveautomation.jdbc.postgresql,
    com.cirruslink.mqtt.engine.gateway,
    dev.bwdesigngroup.flint.FlintDesignerBridge,
    com.mussonindustrial.embr.periscope,
    com.mussonindustrial.embr.charts,
    com.mustry.historian.timescaledb
  # Embr-Periscope, Embr-Charts and the TimescaleDB Historian are the three
  # spare modules Part 3 installs. Their .modl files sit in
  # third-party-modules/, but they ship with NO services/modules.json entry
  # AND deliberately absent from the three module env vars here — so a fresh
  # boot neither loads nor trusts them. Part 3 is the student adding both
  # halves: the three module ids to GATEWAY_MODULES_ENABLED +
  # ACCEPT_MODULE_LICENSES + ACCEPT_MODULE_CERTS, and the minimal manifest
  # entry per module (filename + onStartup). On the next boot the gateway
  # installs them headlessly and WRITES the derived certFingerprint +
  # licenseAgreementHash back into modules.json — which the student commits
  # so image-based/fresh gateways accept them from config alone. That
  # derivation is the whole point of Part 3. (MQTT Engine stays pre-wired:
  # it is a 55 MB module and slow to boot, so it is not part of the
  # exercise.)
  ACCEPT_MODULE_LICENSES: >-
    com.cirruslink.mqtt.engine.gateway,
    dev.bwdesigngroup.flint.FlintDesignerBridge,
    com.mussonindustrial.embr.periscope,
    com.mussonindustrial.embr.charts,
    com.mustry.historian.timescaledb
  ACCEPT_MODULE_CERTS: >-
    com.cirruslink.mqtt.engine.gateway,
    dev.bwdesigngroup.flint.FlintDesignerBridge,
    com.mussonindustrial.embr.periscope,
    com.mussonindustrial.embr.charts,
    com.mustry.historian.timescaledb
  GATEWAY_RESTORE_DISABLED: "true"
  GATEWAY_NETWORK_ENABLED: "true"
  TZ: "Europe/Brussels"

# Run the gateway under umask 0002. The image's default umask (022) makes
# everything the gateway writes into the bind mounts 644/755 — group
# read-only — so despite group_add + setgid the student cannot edit or
# delete those files (and teardown.sh --volumes fails on them). With 0002
# they land 664/775, group-owned by the student via the setgid dirs.
x-ignition-entrypoint: &ignition-entrypoint
  ["/bin/sh", "-c", 'umask 0002 && exec docker-entrypoint.sh "$@"', "--"]

x-ignition-resources: &ignition-resources
  deploy:
    resources:
      limits:
        memory: 1G
      reservations:
        memory: 512M
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"

x-ignition-healthcheck: &ignition-healthcheck
  test: ["CMD", "curl", "-fsS", "http://localhost:8088/StatusPing"]
  interval: 30s
  timeout: 10s
  retries: 10
  start_period: 90s

secrets:
  postgres_password:
    file: ./secrets/postgres_password.txt
  reporting_password:
    file: ./secrets/reporting_password.txt

services:
  # --- local gateway: edits on the host show up here immediately --------------
  gateway-local-development:
    image: inductiveautomation/ignition:8.3.8
    container_name: lab06-gateway-local-development
    hostname: lab06-gateway-local-development
    # Keep the image's own identity (uid 2003, gid 0): its data/ tree is
    # owned 2003:0 mode 775, so any OTHER primary gid loses write access and
    # the gateway fails to boot ("gateway.xml exists, but isnt readable or
    # writable"). Instead we ADD the student's group, which -- together with
    # the group-write + setgid bits scripts/setup.sh applies to the
    # bind-mounted dirs -- makes everything the gateway writes editable by
    # the student. No root-owned files, so no sudo.
    # LAB_GID is exported by scripts/preflight.sh.
    user: "2003:0"
    group_add:
      - "${LAB_GID:-0}"
    entrypoint: *ignition-entrypoint
    restart: unless-stopped
    secrets:
      - postgres_password
      - reporting_password
    ports:
      - "8088:8088"
      - "8060:8060"
    volumes:
      - gateway-local-data:/usr/local/bin/ignition/data
      - ./projects:/usr/local/bin/ignition/data/projects
      - ./services/config:/usr/local/bin/ignition/data/config
      - ./services/modules.json:/usr/local/bin/ignition/data/modules.json
      - ./third-party-modules:/third-party-modules
    environment:
      <<: *ignition-env
      GATEWAY_ADMIN_USERNAME: "${GATEWAY_ADMIN_USERNAME_LOCAL:-admin}"
      GATEWAY_ADMIN_PASSWORD: "${GATEWAY_ADMIN_PASSWORD_LOCAL:-password}"
      # The LOCAL gateway runs with CUSTOM secrets-management keys
      # (services/config/ignition/keys/ — committed, dummy lab keys) instead
      # of the default key every Ignition installation shares. That's what
      # makes the committed embedded ciphertexts decrypt HERE and nowhere
      # else: .deployignore keeps the keys out of the deploy payload, so
      # test/production fall back to their own keys and the connections fault —
      # the warm-up's whole point. This passphrase unlocks the root key.
      IGNITION_ROOT_KEY_PASSWORD: "${IGNITION_ROOT_KEY_PASSWORD:-lab06-root-key-pass}"
    command: >
      -n lab06-local
      -m 1024
      --
      wrapper.java.initmemory=256
      -Dignition.allowunsignedmodules=true
      -Dignition.gateway.externalModulesFolder=/third-party-modules
      -Dignition.config.mode=local-development
    healthcheck: *ignition-healthcheck
    <<: *ignition-resources
    networks:
      - default
    depends_on:
      timescaledb:
        condition: service_healthy

  # --- test gateway: populated by deploy.yml (push to main) -------------------
  gateway-test:
    image: inductiveautomation/ignition:8.3.8
    container_name: lab06-gateway-test
    hostname: lab06-gateway-test
    # Keep the image's own identity (uid 2003, gid 0): its data/ tree is
    # owned 2003:0 mode 775, so any OTHER primary gid loses write access and
    # the gateway fails to boot ("gateway.xml exists, but isnt readable or
    # writable"). Instead we ADD the student's group, which -- together with
    # the group-write + setgid bits scripts/setup.sh applies to the
    # bind-mounted dirs -- makes everything the gateway writes editable by
    # the student. No root-owned files, so no sudo.
    # LAB_GID is exported by scripts/preflight.sh.
    user: "2003:0"
    group_add:
      - "${LAB_GID:-0}"
    entrypoint: *ignition-entrypoint
    restart: unless-stopped
    ports:
      - "8089:8088"
      - "8061:8060"
    volumes:
      - gateway-test-data:/usr/local/bin/ignition/data
      # projects/ and config/ are bind-mounted from ./gateways/test (gitignored,
      # created by setup.sh) so you can SEE what a deploy shipped straight from
      # your laptop: `ls gateways/test/projects`. The rest of data/ (internal db,
      # var/, certificates/) stays in the named volume above.
      - ./gateways/test/projects:/usr/local/bin/ignition/data/projects
      - ./gateways/test/config:/usr/local/bin/ignition/data/config
      # The module manifest is a PER-GATEWAY copy (seeded by setup.sh,
      # updated by deploy.yml) — NOT the repo's services/modules.json, which
      # only the local gateway mounts. If test shared the working-tree file,
      # a local edit would leak into test's next boot without a deploy, and
      # the deploy workflow couldn't tell "already deployed" from "changed".
      - ./gateways/test/modules.json:/usr/local/bin/ignition/data/modules.json
      - ./third-party-modules:/third-party-modules
    environment:
      <<: *ignition-env
      GATEWAY_ADMIN_USERNAME: "${GATEWAY_ADMIN_USERNAME_TEST:-admin}"
      GATEWAY_ADMIN_PASSWORD: "${GATEWAY_ADMIN_PASSWORD_TEST:-password}"
    command: >
      -n lab06-test
      -m 1024
      --
      wrapper.java.initmemory=256
      -Dignition.allowunsignedmodules=true
      -Dignition.gateway.externalModulesFolder=/third-party-modules
      -Dignition.config.mode=test
    healthcheck: *ignition-healthcheck
    <<: *ignition-resources
    networks:
      - default
    depends_on:
      timescaledb:
        condition: service_healthy

  # --- production gateway: populated by deploy.yml (dispatch, target=production) ------------------
  gateway-production:
    image: inductiveautomation/ignition:8.3.8
    container_name: lab06-gateway-production
    hostname: lab06-gateway-production
    # Keep the image's own identity (uid 2003, gid 0): its data/ tree is
    # owned 2003:0 mode 775, so any OTHER primary gid loses write access and
    # the gateway fails to boot ("gateway.xml exists, but isnt readable or
    # writable"). Instead we ADD the student's group, which -- together with
    # the group-write + setgid bits scripts/setup.sh applies to the
    # bind-mounted dirs -- makes everything the gateway writes editable by
    # the student. No root-owned files, so no sudo.
    # LAB_GID is exported by scripts/preflight.sh.
    user: "2003:0"
    group_add:
      - "${LAB_GID:-0}"
    entrypoint: *ignition-entrypoint
    restart: unless-stopped
    ports:
      - "8090:8088"
      - "8062:8060"
    volumes:
      - gateway-production-data:/usr/local/bin/ignition/data
      # Same pattern as test: deployed files are visible on the host under
      # ./gateways/production (gitignored, created by setup.sh).
      - ./gateways/production/projects:/usr/local/bin/ignition/data/projects
      - ./gateways/production/config:/usr/local/bin/ignition/data/config
      # Per-gateway manifest copy — same reasoning as test.
      - ./gateways/production/modules.json:/usr/local/bin/ignition/data/modules.json
      - ./third-party-modules:/third-party-modules
    environment:
      <<: *ignition-env
      GATEWAY_ADMIN_USERNAME: "${GATEWAY_ADMIN_USERNAME_PRODUCTION:-admin}"
      GATEWAY_ADMIN_PASSWORD: "${GATEWAY_ADMIN_PASSWORD_PRODUCTION:-password}"
    command: >
      -n lab06-production
      -m 1024
      --
      wrapper.java.initmemory=256
      -Dignition.allowunsignedmodules=true
      -Dignition.gateway.externalModulesFolder=/third-party-modules
      -Dignition.config.mode=production
    healthcheck: *ignition-healthcheck
    <<: *ignition-resources
    networks:
      - default
    depends_on:
      timescaledb:
        condition: service_healthy

  # --- shared TimescaleDB with three logical databases ----------------------
  # POSTGRES_DB is the database created automatically at first init. The init
  # script under ./db-init/ (mounted below) creates the other two so each
  # gateway can point at its own database, plus the read-only `reporting`
  # login used by the TimescaleDB_Reports gateway connection.
  #
  # Both passwords below are plain environment variables — the rung-1 leak
  # Part 1A of the lab starts from (`docker inspect lab06-timescaledb | grep
  # PASSWORD`). The db-init script also understands REPORTING_PASSWORD_FILE,
  # so the same _FILE convention as POSTGRES_PASSWORD_FILE works once you
  # move to file-based secrets.
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: lab06-timescaledb
    restart: unless-stopped
    secrets:
      - postgres_password
      - reporting_password
    ports:
      - "5432:5432"
    volumes:
      - timescaledb-data:/var/lib/postgresql/data
      - ./db-init:/docker-entrypoint-initdb.d:ro
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "10"
    environment:
      POSTGRES_USER: "${POSTGRES_USER:-ignition}"
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
      POSTGRES_DB: ignition_local_development
      REPORTING_PASSWORD_FILE: /run/secrets/reporting_password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ignition}"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - default

  # --- self-hosted GitHub Actions runner -------------------------------------
  # Auto-registers against the repo set in RUNNER_REPO_URL using a GitHub
  # Personal Access Token (RUNNER_GITHUB_PAT, must have `repo` scope). The
  # image uses that PAT to fetch its own short-lived registration token, so
  # you never mint one by hand. Reuse the same PAT you created in Lab 04.
  #
  # The runner sits on the lab's compose network, so it reaches the gateways by
  # service name (gateway-local-development, gateway-test, gateway-production) — see the
  # IGNITION_URL defaults in .github/workflows/deploy.yml.
  #
  # Why /var/run/docker.sock is mounted: deploy.yml uses
  # `docker cp` and `docker exec` to push project files INTO the test/production
  # gateway containers. That requires the runner to talk to the host's
  # Docker daemon. Lab convenience, NOT a production pattern.
  github-runner:
    image: myoung34/github-runner:latest
    container_name: lab06-runner
    hostname: lab06-runner
    restart: unless-stopped
    environment:
      REPO_URL: "${RUNNER_REPO_URL}"
      ACCESS_TOKEN: "${RUNNER_GITHUB_PAT}"
      RUNNER_NAME: "${RUNNER_NAME:-lab06-runner}"
      RUNNER_SCOPE: repo
      LABELS: "self-hosted,lab06"
      # No EPHEMERAL here on purpose: the image treats ANY non-empty value
      # (even "false") as "enable --ephemeral". Leave it unset for a
      # persistent runner.
      DISABLE_AUTO_UPDATE: "true"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - default

volumes:
  gateway-local-data:
  gateway-test-data:
  gateway-production-data:
  timescaledb-data:
